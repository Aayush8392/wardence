# Private working notes -- never committed
wardence_context.md
wardence_buildlog.md
wardence_frontend.md
wardence_guide.html
reviews/

# Raw Stitch design exports (zips/code/screenshots) -- reference material
# for building the real frontend, not app code itself. One subfolder per
# export, named for what it's a mockup of.
p4_frontend/stitch_mockups/

# Python
p2_readonly_loop/venv/
__pycache__/
*.pyc

# Stale local DB folder from before the DB moved to WSL2's native filesystem
p2_readonly_loop/output/

# One-off diagnostic/probe scripts
traffic_gen/repro_session_race.py
p3_trust_action/stitch_data_sample.py
stitch_sample_output.txt

# Live ServiceAccount credential -- never committed
p3_trust_action/sa_token.txt

# JWT signing secret -- never committed
p3_trust_action/jwt_secret.txt

# R2 credentials -- never committed
p3_trust_action/.env
