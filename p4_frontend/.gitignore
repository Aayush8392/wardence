# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Not secret (R2 base URL is a public read-only endpoint) but kept out of
# git anyway for per-environment flexibility, consistent with the rest of
# the repo's .env convention (p3_trust_action/.env).
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
